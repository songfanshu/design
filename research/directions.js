const box=document.querySelector('.rd-lightbox');if(box){const content=box.querySelector('.rd-lightbox-content');document.querySelectorAll('.rd-figure>a,.rd-diagram-open').forEach(trigger=>trigger.addEventListener('click',event=>{if(!box.showModal)return;event.preventDefault();content.replaceChildren(trigger.querySelector('img,svg').cloneNode(true));box.showModal();}));box.querySelector('.rd-lightbox-close').addEventListener('click',()=>box.close());box.addEventListener('click',event=>{if(event.target===box)box.close();});box.addEventListener('close',()=>content.replaceChildren());}