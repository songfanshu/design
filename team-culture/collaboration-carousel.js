(()=>{const carousel=document.querySelector('.carousel'),slides=[...document.querySelectorAll('.slide')],dots=[...document.querySelectorAll('.dots button')];let current=0,timer;function show(n){current=(n+slides.length)%slides.length;slides.forEach((s,i)=>{s.classList.toggle('active',i===current);s.setAttribute('aria-hidden',String(i!==current));s.inert=i!==current});dots.forEach((d,i)=>{d.classList.toggle('active',i===current);d.setAttribute('aria-pressed',String(i===current))})}function stop(){clearInterval(timer)}function start(){stop();if(document.hidden||carousel.matches(':hover')||carousel.contains(document.activeElement))return;timer=setInterval(()=>show(current+1),5000)}document.querySelector('.prev').onclick=()=>{show(current-1);start()};document.querySelector('.next').onclick=()=>{show(current+1);start()};dots.forEach((d,i)=>d.onclick=()=>{show(i);start()});carousel.addEventListener('mouseenter',stop);carousel.addEventListener('mouseleave',start);carousel.addEventListener('focusin',stop);carousel.addEventListener('focusout',()=>requestAnimationFrame(start));document.addEventListener('visibilitychange',start);carousel.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key==='ArrowLeft'){e.preventDefault();show(current+(e.key==='ArrowRight'?1:-1));start()}});let touch;carousel.addEventListener('touchstart',e=>{touch={x:e.touches[0].clientX,y:e.touches[0].clientY};stop()},{passive:true});carousel.addEventListener('touchend',e=>{if(!touch)return;const dx=e.changedTouches[0].clientX-touch.x,dy=e.changedTouches[0].clientY-touch.y;if(Math.abs(dx)>50&&Math.abs(dx)>Math.abs(dy)*1.5)show(current+(dx<0?1:-1));touch=null;start()},{passive:true});show(0);start()})();